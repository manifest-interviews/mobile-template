# @ts-rest/react-query@3.53.x ships a stale peer range (React <=18). React 19 +
# react-query v5 + ts-rest works fine, so we relax peer resolution to keep
# `npm install` a one-shot command. Remove this if ts-rest updates the range.
legacy-peer-deps=true
